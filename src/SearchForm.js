import React, { Fragment } from "react";

const SearchForm = () => {
  return (
    <Fragment>
      <form>
        <input />
        <button>Search</button>
      </form>
    </Fragment>
  );
};

export default SearchForm;
